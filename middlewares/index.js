const validation = require("./validation");
const authenticate = require("./authenticate");
const upload = require("./upload");
const size = require("./size");
module.exports = { validation, authenticate, upload, size };
